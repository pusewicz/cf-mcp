#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Signatures for gems that ship them in the RBS collection (used by Steep and the tests)
bundle exec rbs collection install

# Do any other automated setup that you need to do here
